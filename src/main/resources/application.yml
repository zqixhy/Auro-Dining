server:
  port: 80
spring:
  main:
    banner-mode: off
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/restaurant
    username: postgres
    password: '7070'
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        globally_quoted_identifiers: true
  redis:
    host: localhost
    port: 6379
    database: 0
    timeout: 3000ms
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms
  cache:
    redis:
      time-to-live: 1800000ms
      cache-null-values: true

auro-dining:
  path: ${user.home}/Pictures/auro_dining_images/

sms:
  provider: log
  fixed-code: 1234

email:
  provider: log
  # For testing: set a fixed code (e.g., "1234") to always use this code
  fixed-code: 1234
  # Sender email address (must be verified in AWS SES for production)
  from: zqixhy@gmail.com

